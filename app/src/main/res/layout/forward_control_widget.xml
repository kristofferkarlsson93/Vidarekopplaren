<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/blue"
    android:padding="@dimen/widget_margin">

    <TextView
        android:id="@+id/widgetTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/widgetStartButton"
        android:layout_centerHorizontal="true"
        android:text="00:00"
        android:textColor="#fff"
        android:textSize="30sp" />

    <Button
        android:id="@+id/widgetStartButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="@drawable/startbutton"
        android:text="Button" />

    <TextView
        android:id="@+id/widgetPhoneNumberText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/widgetTime"
        android:layout_alignBottom="@+id/widgetTime"
        android:layout_alignParentStart="true"
        android:layout_marginStart="15dp"
        android:textColor="#fff"
        android:textSize="30sp" />

</RelativeLayout>